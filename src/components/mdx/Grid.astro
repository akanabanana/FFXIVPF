---
const props = Astro.props;

interface Props {
  class?: string;
  cols?: string;
}
---

<div {...props} class="grid gap-4 { props.class || '' } { `grid-cols-${props.cols}` || 'grid-cols-2' }">
  <slot />
</div>
